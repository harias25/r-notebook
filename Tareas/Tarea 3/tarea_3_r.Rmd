---
title: "Tarea 3"
output: html_notebook
name: "Haroldo Pablo Arias Molina"
id: "999008683"
---

# Tarea 3

### Información General
- Nombre: Haroldo Pablo Arias Molina
- Registro: 999008683

# LECTURA DEL ARCHIVO
```{r}
library(tidyverse)
primary_results <- read_csv("./primary_results.csv",show_col_types = FALSE)
print(primary_results)
```



# 1. Filtrar todos los votos del estado de Florida.

```{r}
filter(primary_results, 
      state=="Florida")
```



# 2. Utilice operadores lógicos (& and |), según corresponda:

### 2.1 Filtrar votos para los candidatos Hillary Clinton o Donald Trump

```{r}
filter(primary_results,
       candidate=="Hillary Clinton" | candidate=="Donald Trump")
```



### 2.2 Filtrar votos para el partido Demócrata y el condado Shelby

```{r}
filter(primary_results,
       party=="Democrat" & county=="Shelby")
```

### 2.3 Filtrar votos para el estado de Alabama y que la cantidad de votos sea mayor a 15000

```{r}
filter(primary_results,
       state=="Alabama" & votes > 15000)
```

# 3. Utilice la función select y arrange para el siguiente filtrado
### 3.1 Filtrar votos para el estado de Texas y el condado Denton, seleccionando únicamente las columnas party, candidate, votes, luego ordene de forma descendente por la columna votes.

```{r}
arrange(select(filter(primary_results,
                      state=="Texas" & county == "Denton"),
                      party,
                      candidate,
                      votes),
                      desc(votes))
```

# 4. Utilice el verbo mutate y arrange para el siguiente filtrado
### 4.1 Filtrar por el partido Republicano, que el candidato sea Ted Cruz en el estado de Alaska, luego agregue una columna que se llame vote_count 
###     que sea la división de votes por fraction_votes (votes/fraction_votes), finalmente ordene de forma descendente por vote_count.

```{r}
arrange(mutate(filter(primary_results, 
                      party == "Republican" & candidate=="Ted Cruz" & state=="Alaska"),
               vote_count=votes/fraction_votes),
               desc(vote_count))
```